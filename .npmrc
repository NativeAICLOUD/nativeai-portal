# React 19 is out ahead of several transitive peerDependency ranges (radix-ui,
# next-auth, framer-motion, etc.) that are known to work at runtime but haven't
# bumped their declared ranges yet. Without this, npm install hard-fails with
# ERESOLVE on those declaration-only mismatches.
legacy-peer-deps=true
